import React, { useRef, useState } from "react";
import { useGSAP } from "@gsap/react";
import gsap from "gsap";
//the way of writing the gsap

const App = () => {




  return (
    <main>
      <button
      >
        Animate
      </button>
      <div className="box"></div>
    </main>
  );
};

export default App;
